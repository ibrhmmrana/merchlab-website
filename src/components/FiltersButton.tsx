'use client';

import { Button } from '@/components/ui/button';

export function FiltersButton() {
  return (
    <Button
      variant="outline"
      className="whitespace-nowrap"
    >
      Filters
    </Button>
  );
}
